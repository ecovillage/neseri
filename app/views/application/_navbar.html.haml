%nav.navbar{"aria-label" => "main navigation", :role => "navigation", class: 'is-light'}
  .navbar-brand
    %a.navbar-burger.burger{"aria-expanded" => "false", "aria-label" => "menu", "data-target" => "navbarBasicExample", :role => "button"}
      %span{"aria-hidden" => "true"}
      %span{"aria-hidden" => "true"}
      %span{"aria-hidden" => "true"}
  #navbar.navbar-menu
    .navbar-start
      %a.navbar-item
        Home
      %a.navbar-item
        Documentation
      = link_to t('pages.contact'), contact_path, class: 'navbar-item'
      = link_to t('pages.impressum'), impressum_path, class: 'navbar-item'
      = link_to t('pages.privacy'), privacy_path, class: 'navbar-item'
      = link_to t('pages.about'),  about_path, class: 'navbar-item'
    .navbar-end
      - if current_user&.admin?
        .navbar-item.has-dropdown.is-hoverable
          %a.navbar-link
            = :admin
          .navbar-dropdown
            - if allowed_to? :index?, User
              = link_to User.model_name.human(count: 2), admin_users_path, class: "navbar-item"
            - if allowed_to? :index?, User
              = link_to Room.model_name.human(count: 2), admin_rooms_path, class: "navbar-item"
            - if allowed_to? :index?, User
              = link_to :settings, admin_users_path, class: "navbar-item"
            - if allowed_to? :index?, User
              = link_to :texts, admin_users_path, class: "navbar-item"
            - if allowed_to? :index?, SeminarKind
              = link_to SeminarKind.model_name.human(count: 2), admin_seminar_kinds_path, class: "navbar-item"
      .navbar-item
        .buttons
          - if user_signed_in?
            = link_to current_user.email, instructor_path, class: 'button'
            = link_to t('sessions.destroy.sign_out'), destroy_user_session_path, method: :delete, class: "button is-light"
          - else
            %a.button.is-primary(href=new_user_registration_path)
              %strong= t('devise.registrations.new.sign_up')
            = link_to t('devise.sessions.new.sign_in'), new_user_session_path, class: 'button'
